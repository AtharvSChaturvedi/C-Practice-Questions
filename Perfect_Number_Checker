#include<stdio.h>
int main() {
	int a,sum=0,i;
	printf("Enter a number = ");
	scanf("%d", &a);
	for(i=1;i<a;i++) {
		if(a%i==0){
			sum=sum+i;
		}
	}
	if(sum==a){
		printf("%d is a perfect number\n",a);
	}
	else {
		printf("%d is not a perfect number\n",a); 
	}
}
